"""Example bake-off profiles (reference configurations, not defaults).

Each subdirectory ships a ``profile.json`` (resolvable by directory name via
``--profile <name>``) and optionally a baseline registry and the dataset
converters for that domain's public benchmarks.
"""
