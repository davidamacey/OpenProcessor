# Core Web Framework
fastapi
uvicorn[standard]
python-multipart
orjson  # 2-3x faster JSON encoding/decoding vs stdlib json
pydantic-settings  # Pydantic v2 settings management

# YOLO and Triton
ultralytics>=8.4.82,<8.5  # 8.4.x = YOLO26 support (native NMS-free export + .pt loading). The legacy YOLO11 EfficientNMS export toolchain is isolated in /opt/venv-y11 (requirements-export-y11.txt) because its patch requires <8.4.
tritonclient[all]

# Model Export Dependencies
onnx>=1.12.0,<1.22  # upper bound tracks nvidia-modelopt; venv-y11 keeps its own tighter pin
onnxslim>=0.1.71
onnxsim>=0.4.33
onnxscript
onnxruntime-gpu
onnx-graphsurgeon  # Required for End2End TensorRT NMS graph manipulation
tensorrt-cu13==11.1.0.106  # MUST match the Triton image TensorRT (11.1.0.106 — asserted at image build) for engine compatibility — bump only together with the Dockerfile.triton TRT_VERSION assertion
nvidia-modelopt[onnx]  # TRT 11 is strongly-typed: FP16 is baked into ONNX via ModelOpt AutoCast at export time

# PyTorch
torch
torchvision

# Image Processing
opencv-python
# Note: Pillow is installed as dependency of ultralytics (no need to specify)

# Scientific Computing
numpy
scipy

# HTTP Client
requests
aiohttp
httpx

# Benchmarking and Visualization
matplotlib
seaborn
pandas
psutil
tqdm
tabulate

# Configuration
pyyaml

# Structured Logging
structlog>=24.1.0  # Structured logging with context and JSON output
tenacity>=8.2  # Retry/backoff for src/services/labeling/vlm_client.py (VLM HTTP calls)

# Metrics
prometheus-client>=0.20  # /metrics exposition + HTTP latency histogram

# OpenSearch for Visual Search
opensearch-py>=2.3.0  # Async OpenSearch client with k-NN support
transformers>=4.30.0  # CLIP tokenizer for text-to-image search

# MobileCLIP dependencies
timm  # pytorch-image-models - required for MobileCLIP architecture
huggingface_hub  # For downloading MobileCLIP checkpoints
ftfy  # Text cleaning for CLIP tokenizer (required by open_clip)
open-clip-torch>=2.24.0  # OpenCLIP with native MobileCLIP2-S2 support

# FAISS Clustering (GPU-accelerated vector clustering)
faiss-gpu-cu12>=1.10.0  # FAISS GPU for billion-scale clustering (CUDA 12)

# Duplicate Detection
imohash  # Fast constant-time image hashing (48KB sampled, ~1ms for any file size)

# Perception Encoder (PE) text tower for text-to-image curation search
# (src/clients/pe_encoder.py, lazily imported in warm_text_encoder()).
# Not on PyPI under a normal name -- the pip distribution is named
# `perception_models`, but the package it installs is top-level `core`
# (verified against facebookresearch/perception_models' own pyproject.toml).
# This was previously undeclared entirely (comment in pe_encoder.py claimed
# it was already here) -- same missing-dependency bug class as the
# clustering packages above, just not one of the four the audit named.
perception_models @ git+https://github.com/facebookresearch/perception_models.git

# Text-detection lean/angle recovery (src/services/detection/region_lean.py,
# lazily imported in default_detector()). Also currently undeclared anywhere;
# the module has no production importer yet (pending the Wave 2 ingest
# wiring), but it is real ported code with a real hard (non-optional) import.
paddleocr

# Curation Clustering (lazily imported -- ImportError only surfaces on first
# call to a clustering endpoint if these are missing from the image)
scikit-learn  # src/services/curation/clustering/orchestrator.py, methods/ahc.py, backend.py; scripts/curation/bakeoff/sample.py
umap-learn  # src/services/curation/clustering/embedding_reduce.py, backend.py; src/services/curation/embedding_viz.py
hdbscan  # src/services/curation/clustering/methods/hdbscan.py
joblib  # src/services/curation/clustering/embedding_reduce.py, embedding_viz.py

# GPU arbiter (src/services/training/gpu_arbiter.py): stops/starts
# GPU-resident sibling containers (docker.sock mount, OP_GPU_ARBITER_*)
# so a training run doesn't start next to another GPU workload on the
# same device. S-5 (full_stack_audit.md): without this package, every
# call falls back to a sentinel-only pause that never actually stops
# anything -- fail-open. Pin the SDK, not "docker" the CLI wrapper.
docker>=7,<8
