"""Generic curation/labeling API routers, mounted under ``CurationConfig.api_prefix``.

Router registration is added incrementally as each router is ported
(``docs/design/oss_genericization_phase2_plan.md`` §5, Chunk 1 onward).
Empty in Chunk 0 — no routers exist under this package yet.
"""
